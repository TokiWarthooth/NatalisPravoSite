name: Deploy to TimeWeb Cloud

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Trigger deployment webhook
      run: |
        curl -X POST http://194.87.200.201:9000/deploy \
          -H "Content-Type: application/json" \
          -d '{"ref": "refs/heads/main", "repository": "NatalisPravoSite"}'
